@{
    ViewData["PanelTitle"] = "تقویم";
}

@section Styles {
    <link rel="stylesheet" href="~/panel-assets/vendor/libs/fullcalendar/fullcalendar.css" />
}

<div class="d-flex flex-wrap justify-content-between align-items-center mb-4 gap-2">
    <h4 class="mb-0"><i class="bx bx-calendar text-primary me-1"></i> تقویم وظایف و رویدادها</h4>
    <div class="d-flex gap-2">
        <a href="/App/m/tasks/create" class="btn btn-outline-primary"><i class="bx bx-plus me-1"></i> وظیفه جدید</a>
        <a href="/App/m/events/create" class="btn btn-primary"><i class="bx bx-plus me-1"></i> رویداد جدید</a>
    </div>
</div>

<partial name="_Alerts" />

<div class="card">
    <div class="card-body">
        <div id="calendar"></div>
    </div>
</div>

@section Scripts {
    <script src="~/panel-assets/vendor/libs/fullcalendar/fullcalendar.js"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function () {
            var el = document.getElementById('calendar');
            if (!el || typeof Calendar === 'undefined') {
                console.error('FullCalendar bundle not loaded.');
                return;
            }

            var calendar = new Calendar(el, {
                plugins: [dayGridPlugin, timegridPlugin, listPlugin],
                initialView: 'dayGridMonth',
                direction: 'rtl',
                locale: 'fa',
                firstDay: 6,
                height: 'auto',
                headerToolbar: { start: 'prev,next today', center: 'title', end: 'dayGridMonth,timeGridWeek,listMonth' },
                buttonText: { today: 'امروز', month: 'ماه', week: 'هفته', list: 'لیست' },
                dayCellContent: function (arg) {
                    return { html: '<div class="fc-daygrid-day-number">' + window.jalaliDayNumber(arg.date) + '</div>' };
                },
                datesSet: function (info) {
                    var titleEl = document.querySelector('#calendar .fc-toolbar-title');
                    if (titleEl) titleEl.textContent = window.jalaliCalendarTitle(info.view.currentStart);
                },
                events: '/App/calendar/feed'
            });
            calendar.render();
        });
    </script>
}
